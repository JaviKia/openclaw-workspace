# 2026-04-20

- Se cerró y empujó la fase 1 de voz web privada para pruebas reales desde portátil/navegador: UI estática en `public/voice-phase1/index.html`, servidor en `scripts/web-phase1/server.mjs`, doc en `docs/voice-web-phase1.md`, commit `72990b4` (`Add private web voice phase 1 prototype`).
- La web de fase 1 usa navegador del portátil para micrófono/altavoz, con STT Whisper en el VPS, backend real OpenClaw, TTS Microsoft y respuesta MP3 de vuelta al navegador. Se validó ida y vuelta real por `POST /api/turn`.
- Quedó resuelto el problema operativo de “herramienta expuesta” para compactar sesiones: se añadió wrapper local `scripts/session-tools/compact-session.mjs` que llama a `sessions.compact` usando el binario bueno `/data/.npm-global/bin/openclaw`, con doc en `docs/session-compaction.md` y scripts rápidos `npm run compact:main`, `compact:runtime:e2e`, `compact:web`.
- Se probó el helper de compactación contra la sesión dedicada `agent:main:runtime-openclaw-e2e` y funcionó.
- Se hizo commit y push del resto pendiente en `d619933` (`Tune hard runtime samples and add compaction helper`), incluyendo ajustes de STT/números, muestras `sample7` a `sample10` y notas/memoria pendientes.
- Javi dejó explícita la regla operativa final: en cada ejecución, antes de cualquier otra cosa, hay que comprobar el contexto actual y, si supera ~50%, compactar primero usando el helper local de sesión. Esto debe tratarse como flujo por defecto, no como sugerencia.
- Se documentó ese flujo también en `TOOLS.md` y se reforzó en `MEMORY.md` para que no vuelva a perderse.
