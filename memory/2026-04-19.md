# 2026-04-19

- Nuevo día de trabajo. Javi recordó explícitamente que hoy ya cuenta como un día nuevo.
- Javi pidió seguir con la implementación del runtime Level 1, priorizando primero el camino 1: hacerlo ejecutable de verdad antes de seguir afinando comportamiento.
- Se añadió playback real inicial con `ffplay` en `packages/playback-runtime`, con parada inmediata y limpieza de temporales. Commit: `c18e26c` `Add initial playback controller`.
- Se añadió audio input real inicial con PulseAudio en `packages/audio-runtime` usando `parec`. Hallazgo importante del host: solo estaba visible `auto_null.monitor`, así que el adaptador quedó listo pero sin fuente/micrófono real útil en ese entorno. Commit: `b3c68d5` `Add initial pulse audio input`.
- Se añadió VAD básico por energía RMS (`EnergyVad`) y se cableó el loop mínimo `audio input -> VAD -> orchestrator -> STT`, dejando la arquitectura base del runtime ya en pie. Commit: `6c5d5c8` `Wire VAD into runtime loop`.
- Se instaló la toolchain mínima del monorepo (`typescript`, `tsx`, `@types/node`), se corrigieron problemas de `tsconfig`/tipos/imports internos y el runtime headless pasó a compilar y arrancar de verdad. Validaciones completadas: `npm run check`, `npm run build`, `npm run start:headless`. Commit: `318cf9d` `Make headless runtime executable`.
- El siguiente paso recomendado quedó fijado como conectar `tts.audio.chunk -> playback` y, después, implementar interrupción real cuando entre nueva voz durante playback.
- Javi corrigió una regla operativa importante: no basta con tener la preferencia de compactar alrededor del 50% de contexto. En cada ejecución hay que comprobar el nivel de contexto actual y, si supera ~50%, compactar antes de seguir trabajando para evitar coste innecesario.
- En la mejora del runner headless para no imprimir `final state` antes de tiempo, el problema real ya no era una carrera de eventos sino un timeout final demasiado corto para muestras largas.
- Queda como ajuste durable subir el valor por defecto de `OPENCLAW_RUNTIME_FINAL_WAIT_MS` de `10000` a `30000` y revalidarlo con la muestra larga antes de cerrar esa mejora.
- El backend real de OpenClaw para el runtime Level 1 dejó de atascarse en `THINKING` tras cablear `EventBus` al `OpenClawBackendAdapter` y puentear la finalización leyendo el transcript JSONL de la sesión para emitir `backend.token` y `backend.completed`.
- La validación real mostró que el pipeline completo ya funciona también con backend OpenClaw: STT correcto, respuesta real del backend, TTS/playback y retorno final a `LISTENING`.
- Se confirmó que sí es viable usar una sesión dedicada persistente para pruebas E2E del runtime, mejor que depender de subagents efímeros cuando se quiera aislar el backend real.
- La necesidad de timeout final para el perfil OpenClaw es mayor que en echo/local porque ahí cuentan también backend real, TTS y playback; quedó como hallazgo durable que `OPENCLAW_RUNTIME_FINAL_WAIT_MS=30000` puede seguir siendo justo y conviene ampliar margen en ese perfil al revalidar.
