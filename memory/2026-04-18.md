# 2026-04-18

- Primer arranque real del workspace y definición de identidad del asistente.
- Javi configuró Telegram para hablar con Kelex Kia mediante el bot `@KelexKiaBot`.
- Se aprobó el emparejamiento de Telegram para la cuenta principal de Javi.
- Se conectó el workspace local a GitHub usando SSH y se hizo el primer push al repo `git@github.com:JaviKia/openclaw-workspace.git`.
- Se personalizaron `SOUL.md`, `AGENTS.md`, `TOOLS.md`, `USER.md` e `IDENTITY.md`.
- Se definió la identidad del asistente como **Kelex Kia**, inspirada en la IA del universo de Superman.
- Se fijó el avatar oficial en `avatars/kelex-avatar.jpg`.
- Javi confirmó su zona horaria como Madrid/España y su preferencia por un tono cercano, directo, útil y con humor neutro.
- Se instaló y configuró transcripción local de voz con `whisper-cpp` + `ggml-base.bin`.
- Se validó la transcripción local con audios reales en español e inglés a través de Telegram.
- Se eliminó `BOOTSTRAP.md` por quedar obsoleto tras completar la configuración inicial.
- Se revisó la seguridad de OpenClaw y se corrigió el riesgo crítico de `tools.elevated.allowFrom.webchat = "*"` en `/data/.openclaw/openclaw.json`.
- La UI queda local por ahora; el warning restante sobre `gateway.trustedProxies` se deja pendiente hasta que haya reverse proxy.
- Tras un reset del entorno, se recuperó el workspace desde GitHub (`git@github.com:JaviKia/openclaw-workspace.git`) y quedó otra vez en `main`.
- Se conservó `.openclaw` local y se dejó backup previo a la recuperación en `/tmp/openclaw-workspace-pre-recovery-20260418-174344`.
- Revisión posterior al reset: el repo quedó bien restaurado, Telegram siguió operativo y los ficheros de auth/modelos siguen presentes.
- Hallazgo importante: `openclaw status` vuelve a marcar como crítico `tools.elevated.allowFrom.webchat = ["*"]`; la nota previa que decía que esto estaba corregido no coincide con la config actual y hay que revisarlo antes de dar la seguridad por buena.
- Javi pidió una regla operativa nueva: cuando el contexto de sesión llegue a aproximadamente 50%, hacer autocompactado y push al repo.
- Se activó Microsoft TTS con la voz `en-US-BrianMultilingualNeural` como preferencia actual para probar tanto castellano como inglés.
- Al retomar Whisper, se recuperó el modelo desde `/data/.openclaw.broken-1776526345/models/whisper/ggml-base.bin`; conviene recordar `/data/.openclaw.broken-1776526345/` como ruta útil de rescate tras resets.
- Preferencia confirmada: si Javi envía voz, Kelex debe responder también por voz y en el mismo idioma del mensaje.
- Whisper quedó recuperado y validado en flujo real de Telegram: hubo que restaurar `ggml-base.bin`, reactivar `tools.media.audio.models` y añadir `-l auto` a `whisper-cli` para que no forzara inglés por defecto.
- La cadena completa de voz quedó funcionando en Telegram: transcripción correcta en castellano e inglés, y respuestas por voz en el mismo idioma.
# 2026-04-18

- Primer arranque real del workspace y definición de identidad del asistente.
- Javi configuró Telegram para hablar con Kelex Kia mediante el bot `@KelexKiaBot`.
- Se aprobó el emparejamiento de Telegram para la cuenta principal de Javi.
- Se conectó el workspace local a GitHub usando SSH y se hizo el primer push al repo `git@github.com:JaviKia/openclaw-workspace.git`.
- Se personalizaron `SOUL.md`, `AGENTS.md`, `TOOLS.md`, `USER.md` e `IDENTITY.md`.
- Se definió la identidad del asistente como **Kelex Kia**, inspirada en la IA del universo de Superman.
- Se fijó el avatar oficial en `avatars/kelex-avatar.jpg`.
- Javi confirmó su zona horaria como Madrid/España y su preferencia por un tono cercano, directo, útil y con humor neutro.
- Se instaló y configuró transcripción local de voz con `whisper-cpp` + `ggml-base.bin`.
- Se validó la transcripción local con audios reales en español e inglés a través de Telegram.
- Se eliminó `BOOTSTRAP.md` por quedar obsoleto tras completar la configuración inicial.
- Se revisó la seguridad de OpenClaw y se corrigió el riesgo crítico de `tools.elevated.allowFrom.webchat = "*"` en `/data/.openclaw/openclaw.json`.
- La UI queda local por ahora; el warning restante sobre `gateway.trustedProxies` se deja pendiente hasta que haya reverse proxy.
- Tras un reset del entorno, se recuperó el workspace desde GitHub (`git@github.com:JaviKia/openclaw-workspace.git`) y quedó otra vez en `main`.
- Se conservó `.openclaw` local y se dejó backup previo a la recuperación en `/tmp/openclaw-workspace-pre-recovery-20260418-174344`.
- Revisión posterior al reset: el repo quedó bien restaurado, Telegram siguió operativo y los ficheros de auth/modelos siguen presentes.
- Hallazgo importante: `openclaw status` vuelve a marcar como crítico `tools.elevated.allowFrom.webchat = ["*"]`; la nota previa que decía que esto estaba corregido no coincide con la config actual y hay que revisarlo antes de dar la seguridad por buena.
- Javi pidió una regla operativa nueva: cuando el contexto de sesión llegue a aproximadamente 50%, hacer autocompactado y push al repo.
- Se activó Microsoft TTS con la voz `en-US-BrianMultilingualNeural` como preferencia actual para probar tanto castellano como inglés.
- Al retomar Whisper, se recuperó el modelo desde `/data/.openclaw.broken-1776526345/models/whisper/ggml-base.bin`; conviene recordar `/data/.openclaw.broken-1776526345/` como ruta útil de rescate tras resets.
- Preferencia confirmada: si Javi envía voz, Kelex debe responder también por voz y en el mismo idioma del mensaje.
- Whisper quedó recuperado y validado en flujo real de Telegram: hubo que restaurar `ggml-base.bin`, reactivar `tools.media.audio.models` y añadir `-l auto` a `whisper-cli` para que no forzara inglés por defecto.
- La cadena completa de voz quedó funcionando en Telegram: transcripción correcta en castellano e inglés, y respuestas por voz en el mismo idioma.
- Se dejó guardada y subida a GitHub la preferencia de usar `en-US-BrianMultilingualNeural` por ahora para ambos idiomas.
- Quedó confirmado que la respuesta por voz en Telegram debe seguir el idioma detectado del audio entrante, no solo el idioma por defecto de la sesión.
- Los cambios de recuperación de TTS y Whisper quedaron versionados y empujados al repo durante esta misma sesión.
- Antes de cerrar el día, Javi dejó anotado el siguiente frente a retomar en otra sesión: trabajar la aplicación de Discord y su integración para que la conversación sea más fluida.
